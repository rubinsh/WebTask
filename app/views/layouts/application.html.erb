<!DOCTYPE html>
<html>
<head>
  <title>WebTask</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
  <%= render 'layouts/stylesheets' %>
</head>
<body>
<div class="container">
  <div id="header" class="span-24">
    <div class="span-17">
      <h1><%= link_to "WebTask", root_path %></h1>
    </div>
    <div id="user_bar" class="span-6 right">
      <% if logged_in? %>
          Logged in as: <%= current_user.email %> |
          <%= link_to "Logout", logout_path %>
      <% else %>
          <%= link_to "Join Tasker", new_user_path %> |
          <%= link_to "Login", login_path %>
      <% end %>
    </div>
    <hr/>
  </div>
  <div id="main" class="span-24">
    <%= content_tag(:p, notice, :class => 'success') if notice.present? %>
    <%= content_tag(:p, alert, :class => 'alert') if alert.present? %>
    <% if logged_in? %>
    <%= render 'layouts/menu_bar' %>
    <div class="span-24">
      <%= render 'layouts/sidebar' %>
      <div class="span-17 last">
        <%= yield %>
      </div>
    </div>
    <% else %>
        <%= yield %>
    <% end %>
  </div>
  <div id="footer">
    <hr/>
    <div class="quiet prepend-10">
      Rubinsh's first ROR application <br/>
      <%= link_to "Beginning Rails 3", "http://beginningrails.com" %>
    </div>
  </div>
</div>
</body>
</html>
