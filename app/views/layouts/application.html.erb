<!DOCTYPE html>
<html>
<head>
  <title>WebTask</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
  <%= render 'layouts/stylesheets' %>
</head>
<body>

<div class="header_background">
  <div class="container">
    <div id="header" class="span-24">
      <div class="span-18 header_logo">
        <!--<h1></h1>-->
      </div>
      <div id="user_bar" class="span-6 right">
        <% if user_signed_in? %>
            <%= link_to "Logout", destroy_user_session_path %>
        <% else %>
            <%= link_to "Join WebTask", new_user_registration_path %> |
            <%= link_to "Login", new_user_session_path %>
        <% end %>
      </div>
    </div>
  </div>
</div>
<div class="container">
  <div id="main" class="span-24 container_12">
    <%= content_tag(:p, notice, :class => 'success') if notice.present? %>
    <%= content_tag(:p, alert, :class => 'alert') if alert.present? %>

    <% #TODO: remove this stupid if!!! %>
    <% if user_signed_in? %>
        <div class="span-24 ui-widget-content ui-corner-all">
          <div class="ui-widget-header ui-corner-top" id="menu_bar">
            <%= render 'layouts/menu_bar' %>
          </div>
          <div>
            <%= render 'layouts/sidebar' %>
            <%= yield %>
          </div>
        </div>
    <% else %>
        <%= yield %>
    <% end %>
  </div>
  <div id="footer" class="span-24">
    <hr/>
    <div class="quiet prepend-10">

      Rubinsh's first ROR application <br/>
      <%= link_to "Beginning Rails 3", "http://beginningrails.com" %>
    </div>
  </div>
</div>
</body>
</html>
